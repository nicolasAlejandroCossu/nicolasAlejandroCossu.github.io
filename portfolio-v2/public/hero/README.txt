Place your parallax layer images here:

  Layer1.png  — closest to camera (foreground, moves most with mouse)
  Layer2.png  — mid-foreground
  Layer3.png  — mid-background
  Layer4.png  — furthest back (background, moves least, light ray source)

PNG with transparency recommended for layers 1–3.
Layer4 should be a full opaque background image.
